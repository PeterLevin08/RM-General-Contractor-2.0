# Replit setup

This project is a dependency-free static HTML/CSS/JavaScript website.

## Run locally in Replit

Use the configured `Start application` workflow, which serves the project root
with Python's built-in HTTP server on port 5000:

```sh
python3 -m http.server 5000 --bind 0.0.0.0
```

There is no build step or package installation required.